<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="hu.nagyi.sixthweekwork">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SixthWeekWork">
        <activity
            android:name=".ModifyAdvertisementActivity"
            android:exported="false" />
        <activity
            android:name=".CreateAdvertisementActivity"
            android:exported="false" />
        <activity
            android:name=".AdvertisementActivity"
            android:exported="false"
            android:theme="@style/Theme.SixthWeekWork.NoActionBar" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>